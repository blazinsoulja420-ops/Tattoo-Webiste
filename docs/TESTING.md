# Testing Strategy

Required layers include domain unit tests, state-machine tests, FTA/ACR/TRS consistency tests, API/database integration tests, authorization and upload security tests, visual regression, transfer scaling/mirroring, cover-up evidence integrity, and failure recovery.

Phase 1A establishes the domain and production-contract tests.
