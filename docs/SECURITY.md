# Security Baseline

Use least privilege, no committed secrets, signed/expiring private asset delivery, secure upload validation, rate limiting, abuse controls, asset-class separation, audit logging, environment separation, and fail-closed access decisions.

The public repository must never contain real customer body images, credentials, private production packs, or restricted evidence.
